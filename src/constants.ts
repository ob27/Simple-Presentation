// Vertical gap between stacked pages on the shared canvas, in diagram px units.
export const PAGE_GAP = 80;

// Horizontal inset of the page stack from the canvas origin.
export const PAGE_X = 0;

export const DEFAULT_PAPER_SIZE = 'A4' as const;
export const DEFAULT_ORIENTATION = 'portrait' as const;
